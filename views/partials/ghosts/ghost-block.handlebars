<li>
	{{name}}

	<button class="change-eaten" data-id="{{id}}" data-neweaten="{{eaten}}">
		{{#if eaten}}Chomp Chomp{{else}}Alive again{{/if}}
	</button>

	<button class="delete-ghost" data-id="{{this.id}}">DELETE!</button>
</li>